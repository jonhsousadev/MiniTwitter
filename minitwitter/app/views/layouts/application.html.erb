    <!DOCTYPE html>
    <html lang="en">
      <head>
          <title>Foundation Example</title>
          <meta charset="utf-8">
          <meta http-equiv="refresh" content="3" >
          <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/foundation/5.5.2/css/foundation.css">
          <script src="http://cdnjs.cloudflare.com/ajax/libs/jquery/2.2.0/jquery.min.js"></script>
          <script src="http://cdnjs.cloudflare.com/ajax/libs/foundation/5.5.2/js/foundation.min.js"></script>
          <script src="https://cdnjs.cloudflare.com/ajax/libs/modernizr/2.8.3/modernizr.js"></script>

        <meta name="viewport" content="width=device-width, initial-scale=1.0" />

        <title>MiniTwitter</title>

        <%= stylesheet_link_tag    "application" %>
        <%= javascript_include_tag "application", 'data-turbolinks-track' => true %>
        <%= csrf_meta_tags %>
      </head>

      <body>

            <div class="panel">
        <div class="row">
              <div class="medium-10 rows columns">
                  <h1>MiniTwitter</h1>
                  <p>Twitter Fake Application</p>
            </div>
              <div class="medium-2 rows columns">
            <% if user_signed_in? %>
              <div>Olá, <%= current_user.name %><%= link_to('Sair do Sistema', destroy_user_session_path, :method => :delete, :class => "button alert", :role => "button") %></div>
           <% end %>
        </div>
    </div>
          </div>

        <%= yield %>

      </body>
    </html>
